# Ocean.xyz Stats Configuration File
# https://ocean.xyz/stats/$bitcoin_address
bitcoin_address=your_payout_address

# Worker names: one line per worker, as many as you want... usually bitcoin_address:worker_name
worker_name=worker1
worker_name=worker2

# Refresh interval in minutes
refresh=1
